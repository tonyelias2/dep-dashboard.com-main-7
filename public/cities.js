function removeNode1() {
    const myDiv = document.getElementById("myId1");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode2() {
    const myDiv = document.getElementById("myId2");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode3() {
    const myDiv = document.getElementById("myId3");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode4() {
    const myDiv = document.getElementById("myId4");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode5() {
    const myDiv = document.getElementById("myId5");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode6() {
    const myDiv = document.getElementById("myId6");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode7() {
    const myDiv = document.getElementById("myId7");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode8() {
    const myDiv = document.getElementById("myId8");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode9() {
    const myDiv = document.getElementById("myId9");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode10() {
    const myDiv = document.getElementById("myId10");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode11() {
    const myDiv = document.getElementById("myId11");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}

function removeNode12() {
    const myDiv = document.getElementById("myId12");
    const parent = myDiv.parentNode;
    parent.removeChild(myDiv);
}